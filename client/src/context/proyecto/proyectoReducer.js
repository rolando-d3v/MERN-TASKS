import {FORMULARIO_PROYECTO} from './types'


export default (state, action) => {
    switch (action.type) {
        case value:
            
            break;
    
        default:
            return state
    }
}
